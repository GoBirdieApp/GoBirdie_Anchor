# Partial-data gate

**Public Grade work still in progress**

Estimation path when a player supplies a **subset of launch-monitor fields** (ball speed, apex, spin, landing angle).
User input is treated as constraints;
the solver reconciles a consistent launch triple via `reconcileLaunch`.

Output profiles are tagged.

See the [root README](../README.md) for the full pipeline.
